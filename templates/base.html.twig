<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
        {% block stylesheets %}
        {% endblock %}
        <link rel="stylesheet" href="{{ asset('assets/css/styles.css') }}">
        <script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}" defer >
        </script>
        {% block javascripts %}
        {% endblock %}
        <script src="{{ asset('assets/js/scripts.js') }}" defer >
        </script>
    </head>
    <body>
        {# <nav class="py-2 bg-dark border-bottom">
            <div class="container d-flex flex-wrap">
                <div class="dropdown">
                    <a class="nav nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Gestion Stock
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdmin">
                        <li><a class="dropdown-item" href="{{ path('admin') }}">Tableau de bord</a></li>
                        <li><a class="dropdown-item" href="#">Menus</a></li>
                    </ul> 
                </div>
                <div class="dropdown">
                        <a class="nav nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Etat Intervention
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdmin">
                            <li><a class="dropdown-item" href="{{ path('app_etat_intervention') }}">Voir fiche</a></li>
                        </ul>
                    
                </div>

                <div class="dropdown">
                        <a class="nav nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Fiche Exploitation
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdmin">
                            <li><a class="dropdown-item" href="{{ path('app_fiche_exploitation') }}">Voir fiche</a></li>
                        </ul>
                    
                </div>

                <div class="dropdown">
                        <a class="nav nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Fiche Intervention Technique
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdmin">
                            <li><a class="dropdown-item" href="{{ path('app_fiche_intervention_technique') }}">Voir fiche</a></li>
                        </ul>
                
                </div>

                <div class="dropdown">
                        <a class="nav nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Fiche de stock
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdmin">
                            <li><a class="dropdown-item" href="{{ path('app_fiche_stock') }}">Voir fiche</a></li>
                        </ul>
                
                </div>

            </div>

        </nav> #}

        <nav class="navbar navbar-expand-lg bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('home') }}">Application Gestion Stock</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item">
                    <a class="nav-link active" aria-current="page"href="{{ path('admin') }}">Tableau de bord</a>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Les fiches
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_etat_intervention') }}">Fiche d'intervention</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_fiche_exploitation') }}">Fiche Exploitation</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_fiche_intervention_technique') }}">Fiche Intervention Technique</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_fiche_stock') }}">Fiche de stock</a></li>
                    </ul>
                    </li>
                    </li>
                    </ul>
                    <div style="margin-left:65%;"> 
                   <ul class="navbar-nav">
    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        {#<li class="nav-item">
            <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
        </li>#}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
        </li>
    {% else %}
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                {{ app.user.username|capitalize }}
            </a>
            <ul class="dropdown-menu">
                {# //<li><a class="dropdown-item" href="{{ path('app_profile' , { username: app.user.username }) }}">Mon profil</a></li>#}
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
            </ul>
        </li>
    {% endif %}
</ul>


                </ul>
                </div>
                </div>
            </div>
            </nav>



        {# ////////////////////////////////////////////////////////////// #}

        {% block body %}{% endblock %}
        <div class="container">
            <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
                <div class="col-md-4 d-flex align-items-center">
                    <span class="copyright">©{{ 'now'|date('Y') }}
                    </span>
                </div>
            </footer>
        </div>
    </body>
</html>

